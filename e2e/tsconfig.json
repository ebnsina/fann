{
	"//": "Playwright's own TypeScript loader cannot resolve SvelteKit's `$app/tsconfig` alias, so the e2e suite carries a plain config of its own. Pointed at by `--tsconfig` in the `test:e2e` script.",
	"compilerOptions": {
		"target": "ES2022",
		"module": "ESNext",
		"moduleResolution": "bundler",
		"lib": ["ES2022", "DOM"],
		"types": ["node"],
		"strict": true,
		"esModuleInterop": true,
		"skipLibCheck": true,
		"resolveJsonModule": true,
		"verbatimModuleSyntax": false,
		"noEmit": true
	},
	"include": ["**/*.ts", "../playwright.config.ts"]
}
